'use strict'

function calculateVolumeAndArea(storona) {

    let storona = a;
        volume = a*a*a;
        area = 6*a*a;

    if (typeof(storona) !==='number' || lengt < 0 || !Number.isInteger(storona))
    {"При вычислении произошла ошибка"}
    else
    { 'Объем куба: `${volume}`, площадь всей поверхности: `${area}`'}
    
calculateVolumeAndArea(5);
console.log(calculateVolumeAndArea);



function calculateVolumeAndArea(a) 
    let volume = 0,
        area = 0;

        volume = a*a*a;    
        area = 6*(a*a); 
        return `Объем куба: ${volume}, площадь всей поверхности: ${area}`'


}
    
calculateVolumeAndArea(5);
console.log(calculateVolumeAndArea());